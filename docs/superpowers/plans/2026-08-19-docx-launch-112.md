# DOCX 上线 112（/parse-pdf 全类型路由 + P0 修复）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 112（128.23.67.112:9301）的 `/parse-pdf` 端点正确解析 .docx（当前误路由进 PDF 管线，PyMuPDF 硬解产出 241 个伪标题的垃圾结构），并修复 issues6 的两个 P0。

**Architecture:** 部署镜像已含完整 docx extractor（/parse-json 实测 OK），只需 serve 层接线：路由从写死 `_pdf_kind()` 改为统一 `_route_source_type()`，`_extract_with_images` 补 DOCX 分支；同时删除 serve 层重复的后处理调用（extractor 自 2d34615 起在 `extract()` 内部已跑统一 postprocess，serve 层再跑一遍属双重执行）。P0 修复在 `postprocess.py`：A1 开头守卫（`split_attachments`）、doc_title 位置约束（`calibrate_levels`）。

**Tech Stack:** Python 3.13（uv 管理）、pytest、FastAPI（uvicorn）、lxml、PyMuPDF。

## Global Constraints

- 测试命令一律 `uv run pytest ...`；全量套件必须保持绿色
- `/parse-pdf`、`/parse-json`、CLI 三种模式契约不变（zip 根目录 `result.md`、同步返回、非 2xx=失败）
- 不改 extractor / postprocess 公开函数签名
- 提交信息中文一行式，末尾 `Co-Authored-By: Claude <noreply@anthropic.com>`
- 部署目标：112 上 `docker compose -f docker-compose.d2c.yml up -d --build`（无 SSH 权限，命令交用户执行）

---

### Task 1: 提交工作区基线（2 个提交）

> 2026-08-20 用户决策：`src/document2chunk/extractors/ocr/_mapping.py` 与 `tests/test_ocr_extractor.py`
> 是另一会话进行中的 OCR 对齐修复（issues5 方案 B，测试红/未完成），**不纳入本次上线**，
> 留在工作区不动、不提交、不 stash。

**Files:**
- Modify（已改未提交）: `src/document2chunk/extractors/docx/parser.py`、`tests/test_docx_ooxml.py`、`issues3.md`
- Add（未跟踪）: `issues4.md`、`issues5.md`、`issues6.md`、`docs/superpowers/plans/2026-08-19-docx-launch-112.md`

**Interfaces:**
- Consumes: 无（基线清理）
- Produces: 干净工作区，后续任务在其上叠加

- [ ] **Step 1: 确认改动范围与测试绿**

```bash
git status --short && git diff --stat
uv run pytest tests/test_docx.py tests/test_docx_ooxml.py tests/test_api.py -q
```
Expected: 58 passed（test_docx_ooxml 32 + test_docx/test_api 26）。
注意：`tests/test_ocr_extractor.py` 当前有 5 个已知失败（外来进行中工作），**不在**本次提交/修复范围

- [ ] **Step 2: 提交 docx TOC SDT 跳过**

```bash
git add src/document2chunk/extractors/docx/parser.py tests/test_docx_ooxml.py
git commit -m "fix(docx): TOC SDT 整体跳过防目录伪标题（docPartGallery + WPS HYPERLINK 式探测）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

- [ ] **Step 3: 提交 issues 跟踪文档**

```bash
git add issues3.md issues4.md issues5.md issues6.md docs/superpowers/plans/2026-08-18-docx-test-recommendations.md docs/superpowers/plans/2026-08-19-docx-launch-112.md
git commit -m "docs: issues3-6 验收记录与计划文档

Co-Authored-By: Claude <noreply@anthropic.com>"
```
（`scripts/_*` 诊断脚本、`scan_result.md`、`spotcheck_out.txt`、`text.md`、3 份对接需求文档、`uv.lock`、`.superpowers/`、以及外来进行中的 `ocr/_mapping.py` + `tests/test_ocr_extractor.py` 保持不提交，不进部署基线）

- [ ] **Step 4: 捕获 PDF 双重后处理基线（Task 5 回归对比用，必须在改 serve.py 之前）**

```bash
uv run python - <<'EOF'
from pathlib import Path
from document2chunk import serve
import zipfile, io
names = ["1-9-其他(PDF)(可选).pdf",
         "1-公路隧道可维护防排水系统新型结构及材料和施工关键技术研究-其他.pdf"]
for name in names:
    data = Path(rf"D:/document2chunk-test/data/{name}").read_bytes()
    z = serve.parse_to_zip(data, name)
    md = zipfile.ZipFile(io.BytesIO(z)).read("result.md").decode("utf-8")
    out = Path(r"C:/Users/dyj/AppData/Local/Temp/d2c_pdf_baseline") / (name + ".md")
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(md, encoding="utf-8")
    print(name, "->", len(md), "chars")
EOF
```
Expected: 两个 md 文件写入 baseline 目录，打印字符数

- [ ] **Step 5: 备份 kxx 上一轮 docx 输出（Task 5 对比用）**

```bash
cp -r "D:/project/kxx/kxx-document-test" "D:/project/kxx/kxx-document-test.bak-20260819"
```
Expected: 备份目录存在（含 19 个子目录）

---

### Task 2: serve.py docx 接线 + 去除双重后处理

**Files:**
- Modify: `src/document2chunk/serve.py`（模块顶部 import、`_extract_with_images`、`parse_to_files`、`parse_to_zip`；删除 `_run_postprocess`）
- Create: `tests/test_serve.py`

**Interfaces:**
- Consumes: `api._route_source_type(source, explicit)`（已存在，explicit=None 时按扩展名/魔数路由，PDF 再细分 editable/scanned）；`DocxExtractor().extract(source, options=None, heuristic_headings=False, image_dir=None)`（已存在，source 接受 bytes/路径）
- Produces: `parse_to_files` / `parse_to_zip` 对 .docx 走 DocxExtractor；serve 层不再调 postprocess（全量恰好 1 次，由 extractor 内部执行）

- [ ] **Step 1: 写失败测试**

创建 `tests/test_serve.py`：

```python
"""serve（/parse-pdf、CLI 共用路径）测试：docx 路由 + 单次后处理。"""

from __future__ import annotations

import io
import zipfile

from document2chunk import serve
from document2chunk.ir import SourceType

from test_docx import make_docx

W = "http://schemas.openxmlformats.org/wordprocessingml/2006/main"

DOC_XML = (
    f'<w:document xmlns:w="{W}"><w:body>'
    '<w:p><w:r><w:t>测试文档正文段落</w:t></w:r></w:p>'
    "</w:body></w:document>"
)
DOCX_BYTES = make_docx(DOC_XML)


def _read_md(zip_bytes: bytes) -> str:
    z = zipfile.ZipFile(io.BytesIO(zip_bytes))
    assert "result.md" in z.namelist(), z.namelist()
    return z.read("result.md").decode("utf-8")


def test_parse_to_zip_docx_avoids_pdf_pipeline(monkeypatch):
    """docx 字节不得进 PDF 管线（现状缺陷：PyMuPDF 硬解 docx 产出伪标题垃圾）。"""
    from document2chunk.extractors.pdf import PdfExtractor

    def _boom(self, source, **kw):  # noqa: ANN001
        raise AssertionError("docx 误入 PDF 管线")

    monkeypatch.setattr(PdfExtractor, "extract", _boom)
    md = _read_md(serve.parse_to_zip(DOCX_BYTES, "t.docx"))
    assert "测试文档正文段落" in md


def test_parse_to_files_docx_routes_by_extension(tmp_path):
    """路径模式按 .docx 扩展名路由 → metadata.source_type == DOCX。"""
    src = tmp_path / "t.docx"
    src.write_bytes(DOCX_BYTES)
    out, imgs = tmp_path / "out", tmp_path / "images"
    doc = serve.parse_to_files(str(src), str(out), str(imgs))
    assert (out / "result.md").read_text(encoding="utf-8").find("测试文档正文段落") >= 0
    assert doc.metadata.source_type == SourceType.DOCX


def test_postprocess_runs_exactly_once(monkeypatch):
    """统一后处理全量恰好 1 次（extractor 内部），serve 层不再叠加第二遍。"""
    import document2chunk.postprocess as pp_mod

    calls: list[int] = []
    orig = pp_mod.postprocess

    def _spy(*a, **kw):
        calls.append(1)
        return orig(*a, **kw)

    monkeypatch.setattr(pp_mod, "postprocess", _spy)
    serve.parse_to_zip(DOCX_BYTES, "t.docx")
    assert len(calls) == 1, f"postprocess 应恰好 1 次，实际 {len(calls)} 次"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_serve.py -v`
Expected: 前两个 FAIL（docx 进 PDF 管线 → 第 1 个触发 AssertionError / 第 2 个 source_type 不是 DOCX），第 3 个 FAIL（调了 2 次）

- [ ] **Step 3: 实现**

`src/document2chunk/serve.py` 三处修改：

(a) 顶部 import 去掉 `_pdf_kind`：

```python
from document2chunk.api import _assemble, _source_name
```

(b) `_extract_with_images` 加 DOCX 分支（OCR 分支之后、PdfExtractor 之前）：

```python
    if st == SourceType.DOCX:
        from document2chunk.extractors.docx import DocxExtractor
        # DOCX 无页几何，page_geometry=None；extract() 内部自跑统一 postprocess
        return DocxExtractor().extract(source, image_dir=image_dir), None
```

(c) `parse_to_files` 与 `parse_to_zip` 里的路由行统一改为（两处同样替换）：

```python
    st = _route_source_type(source, source_type)
```

（原：`st = _route_source_type(source, source_type) if source_type else _pdf_kind(source)`；`_route_source_type` 在 explicit=None 时已按扩展名/魔数路由，PDF 再细分 editable/scanned）

(d) 删除 `_run_postprocess` 函数定义及其在 `parse_to_files`、`parse_to_zip` 中的 2 处调用（extractor 自 2d34615 起内部已跑统一 postprocess，参数更全：PDF 路径带 layout_data + use_height_fallback=False）。同步更新模块 docstring 第一段为：

```python
"""serve —— /parse-pdf 接口 + CLI（Chai 复杂解析对接，designs 对接文档）。

三种模式（与 /parse 契约一致，仅产物形式不同）：
- 路径模式：收 file_path/output_dir/image_dir → 写 result.md + images/，返回 {"status":"ok"}
- zip 模式：收文件二进制 → 返回 zip 流（result.md + images/）
- CLI：python -m document2chunk cli --input X --output Y --images Z → 写 result.md + images/

全类型路由（pdf/docx/图片，按扩展名或魔数）；后处理由各 extractor 内部统一执行
（designs/009），serve 层不再叠加。result.md 为**全文**（主文 + 附件）。
"""
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_serve.py tests/test_api.py -q`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add src/document2chunk/serve.py tests/test_serve.py
git commit -m "fix(serve): /parse-pdf 全类型路由（docx→DocxExtractor）+ 去除双重后处理

docx 此前误入 PDF 管线（PyMuPDF 硬解，伪标题垃圾结构，实测 241 伪标题 vs 37 真标题）；
后处理自 2d34615 起由 extractor 内部执行，serve 层第二遍属双重执行。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: P0 #1 — A1 文档开头守卫（split_attachments）

**Files:**
- Modify: `src/document2chunk/postprocess.py:827`（`split_attachments`）
- Test: `tests/test_postprocess.py`（`test_split_attachments_none` 之后追加）

**Interfaces:**
- Consumes: 既有 `split_attachments(content, *, page_geometry=None, _log=None)` 签名不变
- Produces: 行为变化——首个附件边界命中时主文侧无任何非空内容块则不拆（模版类文档整件归主文）

- [ ] **Step 1: 写失败测试**

在 `tests/test_postprocess.py` 的 `test_split_attachments_none` 后追加：

```python
def test_split_attachments_guard_empty_main():
    """A1 文档开头守卫（issues6 P0 #1）：首段即「附件1」的模版类文档不拆，
    整件归主文（此前主文 0 blocks、整文件成 attach1）。"""
    content = [H("附件1：可行性研究报告", level=1), P("模版正文"), P("表格说明")]
    main, attach = split_attachments(content)
    assert attach == []
    assert [b.text for b in main] == ["附件1：可行性研究报告", "模版正文", "表格说明"]


def test_split_attachments_guard_only_blocks_first_boundary():
    """守卫只挡首个边界：其后出现的附件边界正常拆。"""
    content = [H("附件1：表A", level=1), P("A内容"), H("附件2：表B", level=1), P("B内容")]
    main, attach = split_attachments(content)
    assert len(attach) == 1 and attach[0][0].text == "附件2：表B"
    assert [b.text for b in main] == ["附件1：表A", "A内容"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_postprocess.py -k "guard" -v`
Expected: 两个 FAIL（现状：无守卫，照拆）

- [ ] **Step 3: 实现**

`split_attachments` 循环中，把

```python
        if regex_ok and not multi_ref:
            segments.append([b])
```

改为：

```python
        if regex_ok and not multi_ref:
            # ⑤ 文档开头守卫（issues6 根因#1）：首个边界命中且主文侧无任何非空
            # 内容块 → 不拆（模版类文档首段即「附件1」，整件本是上级文档的附件，
            # 拆走致主文 0 blocks）
            if len(segments) == 1 and _nonempty_count(segments[0]) == 0:
                segments[-1].append(b)
                continue
            segments.append([b])
```

并在 `split_attachments` 定义前加模块级辅助：

```python
def _nonempty_count(seg: List[BlockNode]) -> int:
    """段内非空文本块数（开头守卫用）。"""
    return sum(1 for x in seg if (getattr(x, "text", "") or "").strip())
```

- [ ] **Step 4: 跑测试确认通过（含既有 3 个 split 测试不回归）**

Run: `uv run pytest tests/test_postprocess.py -q`
Expected: 全部 PASS（`test_split_attachments_single` 主文侧 2 块、`test_split_attachments_multiple` 主文侧 1 块，均 ≥1，不受守卫影响）

- [ ] **Step 5: 提交**

```bash
git add src/document2chunk/postprocess.py tests/test_postprocess.py
git commit -m "fix(postprocess): A1 文档开头守卫——首边界主文侧空则不拆（issues6 P0 #1）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: P0 #2 — doc_title 位置约束（calibrate_levels）

**Files:**
- Modify: `src/document2chunk/postprocess.py`（`_TITLE_KEYWORDS` 附近加常量；`calibrate_levels` 的 0c/0d 检测块之后、`has_doc_title` 赋值之前插过滤）
- Test: `tests/test_postprocess.py`（calibrate 区块追加）

**Interfaces:**
- Consumes: 既有 `calibrate_levels(...)` 签名不变；`_detect_doc_title_indices` 不改
- Produces: doc_title 候选限制在文档前 `_DOC_TITLE_MAX_BLOCK` 块；前 K 块无候选时保留原候选集

- [ ] **Step 1: 写失败测试**

在 `test_calibrate_doc_title_fallback_no_height` 后追加：

```python
def test_calibrate_doc_title_position_constraint():
    """doc_title 位置约束（issues6 P0 #2）：候选限文档前 K 块——FAQ 类文档
    文中无编号长问句不得凭长度赢过文档开头的短真标题。"""
    long_q = "单位科技管理员或领导在审核过程中发现的问题应当如何进行处理和反馈呢"
    content = [
        H("常见问题解答", level=1, bbox=(0, 0, 100, 40)),
        P("问题一", bbox=(0, 0, 100, 20)),
    ] + [P(f"答案要点{i}", bbox=(0, 0, 100, 20)) for i in range(10)] + [
        H(long_q, level=1, bbox=(0, 0, 100, 40)),
        P("答案", bbox=(0, 0, 100, 20)),
    ]
    md = _md()
    out = calibrate_levels(content, md)
    assert md.title == "常见问题解答"
    # 长问句未被卷入 doc_title 竞争：保持 Heading，不被降级成 Paragraph
    assert long_q in [b.text for b in out if isinstance(b, HeadingNode)]


def test_calibrate_doc_title_position_fallback_keeps_late():
    """前 K 块无候选时保留原候选集（标题确实靠后的文档不丢 doc_title）。"""
    late_title = "某某关于加大耕地提质改造工作力度的实施意见全文标题"
    content = [P(f"版头第{i}行", bbox=(0, 0, 100, 20)) for i in range(12)] + [
        H(late_title, level=1, bbox=(0, 0, 100, 40)),
        P("正文", bbox=(0, 0, 100, 20)),
    ]
    md = _md()
    out = calibrate_levels(content, md)
    assert md.title == late_title
    assert late_title in [b.text for b in out if isinstance(b, HeadingNode)]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_postprocess.py -k "position" -v`
Expected: 第 1 个 FAIL（`md.title == 长问句`，长度碾压）；第 2 个 PASS（现状本就保留，作回归锚）

- [ ] **Step 3: 实现**

(a) `_TITLE_KEYWORDS`（postprocess.py:134）之后加常量：

```python
# doc_title 候选的文档位置上限（块索引）：FAQ 类文档全篇问句挂大字样式，
# 文中长问句凭长度碾压开头短真标题（issues6 根因#2）
_DOC_TITLE_MAX_BLOCK = 10
```

(b) `calibrate_levels` 中，`has_doc_title = len(doc_title_indices) > 0` 一行之前插入：

```python
    # 0e. 位置约束（issues6 根因#2）：doc_title 候选限文档前 K 块；前 K 块无候选
    # 时保留原候选集（版头占多块/标题靠后的文档不丢 doc_title）
    if doc_title_indices:
        early = [i for i in doc_title_indices if i < _DOC_TITLE_MAX_BLOCK]
        if early:
            doc_title_indices = early
```

- [ ] **Step 4: 跑测试确认通过（含既有 doc_title 测试不回归）**

Run: `uv run pytest tests/test_postprocess.py tests/test_docx.py -q`
Expected: 全部 PASS（`test_calibrate_doc_title_secondary_to_custom` 两候选均在块 0/1 <10，不受影响）

- [ ] **Step 5: 提交**

```bash
git add src/document2chunk/postprocess.py tests/test_postprocess.py
git commit -m "fix(postprocess): doc_title 候选限文档前 10 块（issues6 P0 #2）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 回归验证（全量测试 + 真实样本）

**Files:** 无代码改动（只读验证 + 产物对比）

**Interfaces:**
- Consumes: Task 1 的 PDF baseline（`C:/Users/dyj/AppData/Local/Temp/d2c_pdf_baseline/`）与 kxx 备份（`D:/project/kxx/kxx-document-test.bak-20260819`）
- Produces: 上线放行结论（预期变化清单核对）

- [ ] **Step 1: 全量测试**

Run: `uv run pytest -q`
Expected: 除 `tests/test_ocr_extractor.py` 的 **5 个已知外来失败**（另一会话进行中的 OCR 修复，非本次范围，stash 验证过 HEAD 全绿）外，其余全部 PASS。出现任何**新**失败即为不通过

- [ ] **Step 2: PDF 去双重后处理对比（与 Task 1 Step 5 基线）**

```bash
uv run python - <<'EOF'
from pathlib import Path
from document2chunk import serve
import zipfile, io
base = Path(r"C:/Users/dyj/AppData/Local/Temp/d2c_pdf_baseline")
names = ["1-9-其他(PDF)(可选).pdf",
         "1-公路隧道可维护防排水系统新型结构及材料和施工关键技术研究-其他.pdf"]
for name in names:
    data = Path(rf"D:/document2chunk-test/data/{name}").read_bytes()
    z = serve.parse_to_zip(data, name)
    new = zipfile.ZipFile(io.BytesIO(z)).read("result.md").decode("utf-8")
    old = (base / (name + ".md")).read_text(encoding="utf-8")
    print(name, "| old:", len(old), "new:", len(new), "| identical:", old == new)
    if old != new:
        (base / (name + ".new.md")).write_text(new, encoding="utf-8")
EOF
```
Expected: identical 或 new 更规整（差异应集中在标题层级——双重定级消失）。若有差异，逐处人工核对方向正确（伪标题减少/标题层级更合理），并抽查 `fc` 输出

- [ ] **Step 3: kxx 24 件 docx 重跑对比**

```bash
uv run python scripts/convert_docx.py
diff -rq "D:/project/kxx/kxx-document-test.bak-20260819" "D:/project/kxx/kxx-document-test" | head -30
```
Expected: 变化文件仅限预期清单——
  - `可行性研究报告-模版` / `研究与试验发展经费预算表及测算说明-模版`（A1 守卫：主文不再 0 blocks）
  - `…常见问题解答（一）` / `附件1：信息平台操作常见问题（汇总）`（doc_title 位置约束：title 换回真标题）
  - 含 TOC SDT 的文档（Task 1 提交的目录跳过）
  其余文件输出应一致；逐个打开变化文件人工确认方向正确

- [ ] **Step 4: 汇总放行结论**

把三步结果（测试计数、PDF 对比、kxx 变化清单）汇总到对话，确认无意外劣化后放行部署

---

### Task 6: 部署指引更新 + 112 上线

**Files:**
- Modify: `DEPLOY_112.md`（增补 docx 验证小节）

**Interfaces:**
- Consumes: Task 2-5 的代码（已在 main）
- Produces: 用户在 112 执行的命令序列 + 部署后远程验证

- [ ] **Step 1: 更新 DEPLOY_112.md**

在「验证」小节的 zip 模式自测之后追加：

```markdown
# docx 验证（/parse-pdf 全类型路由：pdf/docx/图片按扩展名或魔数自动分流）
curl -X POST http://128.23.67.112:9301/parse-pdf \
  -F "file=@/path/to/test.docx" -o /tmp/r.zip
python -c "import zipfile; z=zipfile.ZipFile('/tmp/r.zip'); print(z.namelist()[:4]); print(z.read('result.md').decode()[:60])"
# 期望：result.md 内容为结构化正文（真标题层级），非表格数字铺标题的劣化输出
```

并把「现状」小节第二行 `/parse-pdf` 说明改为：`/parse-pdf`（d2c 引擎，全类型路由：PDF/DOCX/图片）。

- [ ] **Step 2: 提交**

```bash
git add DEPLOY_112.md
git commit -m "docs(deploy): DEPLOY_112 增补 docx 上线验证（/parse-pdf 全类型路由）

Co-Authored-By: Claude <noreply@anthropic.com>"
git push origin main
```

- [ ] **Step 3: 用户在 112 执行（我无 SSH 权限，命令交给用户）**

```bash
ssh hpc
cd /path/to/document2chunk          # 112 上仓库实际路径
git pull origin main
docker compose -f docker-compose.d2c.yml up -d --build
docker compose -f docker-compose.d2c.yml logs -f document2chunk   # 确认启动后 Ctrl-C
curl http://128.23.67.112:9301/health    # {"status":"ok",...}
```

- [ ] **Step 4: 部署后远程验证（本机 curl，9301 可达）**

```bash
F="D:/document2chunk-test/docx/4- 高性能抗滑封层技术的研究与应用-修改后的研究与试验发展经费投入预算说明书word（含年度经费投入计划）（必须）.docx"
curl -s -m 180 -X POST http://128.23.67.112:9301/parse-pdf -F "file=@$F" -o "$TMP/deploy_check.zip" -w "HTTP %{http_code} size=%{size_download}\n"
```
然后校验：zip 含根目录 `result.md`；内容字数 ≈16K（PDF 管线劣化输出仅 ~7K）；标题数 ≈37（劣化输出 241）；无 `## 324.01` 类数字标题
另抽 1 个 PDF 复测 `/parse-pdf`，确认 PDF 路径不回归（与 Task 5 Step 2 本地输出一致性抽查）

- [ ] **Step 5: 通知 Chai 对接方（如需）**

`chai.http-parse.base-url` / `chai.mineru.base-url` 配置不变（仍是 `http://128.23.67.112:9301/parse-pdf`），docx 文件直接上传即可，无需配置改动

---

## Self-Review 记录

- **覆盖核对**：用户两项决策（/parse-pdf 全类型路由 → Task 2；接线+P0+提交 → Task 1-4）均有对应任务；issues6 分诊表「立即修」两项 → Task 3/4；回归 → Task 5；部署 → Task 6 ✓
- **风险点**：Task 5 Step 2 若 PDF 输出意外劣化（双重后处理 Removal 的历史动机 e8b3478 与时间线矛盾），以基线对比为准，劣化则回滚 Task 2 的 (d) 步并改用「serve 跳过而非 extractor 跳过」方案重新评估——在此显式记录
- **类型一致性**：`_nonempty_count`、`_DOC_TITLE_MAX_BLOCK`、测试 helper（`make_docx`、`P/H/_md`）均来自现有文件，名称核对无误 ✓
