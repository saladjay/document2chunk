# issues8 — 表格嵌入格式实验 open issues（等外部输入）

**日期**: 2026-09-21
**来源**: grill 会话（HTML 表格×embedding 召回论断核查 → 实验立项）；裁定全文与引用 URL 待落 `Document2Chunk-表格嵌入格式-裁定与实验设计.md`
**背景**: markdown 导出中含合并格的表格 chunk 现为带标签 HTML（`export/_helpers.py:113-135` 三分流，DOCX/PDF/OCR 共用），下游原样嵌入。arXiv 2604.24040 实证序列化格式是检索的一阶变量（bge-m3 上格式间 Recall@1 极差 0.264，为四模型最大）；嵌入侧 HTML vs MD 公开头对头实验**不存在**，本地实验是唯一裁决路径。
**状态图例**: ⏸ 等外部输入 | ▢ 可自主开工

---

## A. ⏸ 真实查询日志接入（Q11 的 (a) 分支，需下游配合）

用户确认：真实查询数据后补。当前查询集用 LLM 生成（40 表×4 问 + 对照 80 问，强制混入同义改写/单位换算/跨行推理），日志到位后走**校准**流程：

### 技术计划

1. **导出**：下游向量库/检索服务导出查询日志（query 文本、时间戳、可选命中 id）；导出前脱敏（公文场景含业务敏感词）。
2. **筛选**：启发式（数字/日期/聚合词/表格实体）+ 人工筛出表格相关查询；目标 ≥50 条。
3. **标注**：人工把查询映射到期望 chunk（日志无 gold 标注，抽样标注即可）。
4. **校准**（不是替换）：用日志查询集重跑已锁定网格（bge-m3 臂），与生成查询集对比**格式排序一致性**（Kendall τ）。τ 高 → 生成集可信，结论维持；τ 低 → 以日志集为准，结论重开。
5. **循环**：日志积累后按季度复跑（低成本，只重嵌入查询端，文档端向量不动）。

### 为什么必须校准

生成查询天然偏向「念表格原文」（词法重叠虚高），会系统性奖励含完整表面文本的变体（如 V2 展开管道表）——这是「召回100%会骗人」在实验设计层的形态。真实日志是唯一能证伪生成集偏差的数据。

---

## B. ✅ 已决议（2026-09-22）：走 112 研发中心模型中转

用户指出 `D:\project\server\研发中心模型中转-OpenAI接口调用文档.md`：qwen 臂 = 中转 `qwen3-vl-embedding-8b`（4096 维，OpenAI 兼容），bge 臂 = 中转 `bge-m3`。本地下载已弃。注意中转是 **VL 多模态版**，与生产 `:9015` 的 Qwen3-Embedding-8B 非同一权重（已记入实验报告有效性注记）。

---

## C. ✅ 已执行（2026-09-22，结果见实验报告 §7）

框架按本文件 C 节执行完毕，**判定：维持现状**（含合并格表格 chunk 继续原样嵌出 HTML，预登记判据不满足）。完整结果、发现与有效性注记：`Document2Chunk-表格嵌入格式-裁定与实验设计.md` §7。

遗留（不阻塞结论）：
- ⏸ A（真实日志校准）照旧——用日志查询集重跑网格、与生成集比对格式排序 Kendall τ；**兼作生成查询词法偏置的量化**（审核指出：生成集偏向词法丰富变体，偏袒现状，判定方向不受威胁但量级解读打折）；
- ⏸ **T2/T3 补测 bge 臂**（审核优先级 1，qwen 臂已完成见审查7）：中转 bge-m3 上游 502（>1h，relay 单路故障，qwen 同端点正常），恢复后 `eval_t2.py --arm bge` 一条命令补齐；
- ▢ 可选补测（审核建议）：论文 Table 4 同构的**列名标签 XML** 变体（`<col1>值</col1>`）——本实验 V4 是坐标属性 XML，「论文 XML 常胜」未在本语料被检验；
- ✅ qwen 臂会话/全量对照两轮（2026-09-22 补齐）：V4 在 qwen 三份查询集上均最优（+2.2/+5.0/+4.1pp@10，均未过线），V6 三集全垫底——「格式×模型绑定」进一步加固，判定不变；
- ▢ 主查询集人工抽查：清单在 `_embedlab/gen/spotcheck_relay.md`（24/60 表=全部 24 张曾有反抄袭标记的表，seed=42 兜底逻辑未启用）。

- **网格 V1–V6**：V1 现状 HTML / V2 展开管道表（span 复制填充转 pipe）/ V3 KV 平铺（轨A 式）/ V4 XML / V5 HTML+前置标题行 / V6 NL 摘要进向量（Unstructured 记载的主流实践；**唯一引入索引期 LLM 依赖的变体，若赢需在决策时权衡该成本**）。
- **口径**：Recall@{1,5,10,15,20} + MRR；检索池 = 样本来源文档**全量 chunk 化真池**，严禁只检索被测表。**服务取数窗口已确认 k=10**（easy-rag/.env `MILVUS_KB_DEFAULT_LIMIT=10`，`MILVUS_KB_EMBEDDING_DIMENSION=1024` 坐实 bge-m3，`DEFAULT_SEARCH_TYPE=hybrid`——解读时须计入稀疏路对格式修正不敏感的论文告诫）。
- **判据（预登记，Q14a）**：某变体在双模型上 Recall@{5,10} 平均提升 ≥3pp，且任一模型劣化 ≤1pp，才切换导出格式。
- **样本**：40 含合并格 + 20 简单表（799 DOCX 生产同源，剔除全空/单列/超长退化表）+ Excel 轨A 2 文件对照；chunk 单元 = 表格块 + 前最近标题行。
- **模型角色（Q7c）**：bge-m3 / qwen3-embedding-8b 均在选型，实验矩阵兼作模型对比；结论要求两模型一致，不一致即「格式选择须绑定模型」的发现。
- **术语**：见根目录 `CONTEXT.md`（表格承载格式 / 嵌入输入格式 / 双表征 chunk）。
