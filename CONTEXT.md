# Document2Chunk

文档→chunk 文本生产管线：把 DOCX/PDF/XLSX 等文档解析为可入向量库的 chunk 文本。本仓库是文本生产者，embedding 由下游消费方完成。

## Language

**表格承载格式**:
表格结构（含合并单元格）在解析产物中的表示形态，以结构保真为首要标准。
_Avoid_: 序列化格式、存储格式

**嵌入输入格式**:
实际喂给 embedding 模型的字符串形态，以召回质量为首要标准，可以与承载格式不同。
_Avoid_: 嵌入格式、向量格式

**双表征 chunk**:
同一 chunk 同时携带承载格式表示与嵌入输入格式文本，二者各司其职（Excel 轨A 的行级 envelope 已是此形态）。
_Avoid_: 双输出、混合格式

**序列化格式**:
含糊的伞形词——凡讨论表格表征，必须明确说的是承载格式还是嵌入输入格式。
_Avoid_: 不带上述限定地单独使用
