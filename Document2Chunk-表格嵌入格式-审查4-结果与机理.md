# Document2Chunk-表格嵌入格式-审查包 4 · 结果与机理分析

## 1. 主结果（merged 决策面，主集 n=134，来源 `results/metrics_relay_bge.json` / `metrics_relay_qwen.json`）

| 变体 | bge r@5 | bge r@10 | qwen r@5 | qwen r@10 | Δ@10 vs 现状 (bge/qwen) |
| --- | --- | --- | --- | --- | --- |
| **V1 现状 HTML** | **0.470** | **0.612** | 0.619 | 0.739 | 基线 |
| V2 展开管道表 | 0.396 | 0.590 | 0.604 | 0.694 | −2.2pp / −4.5pp |
| V3 KV 平铺 | 0.358 | 0.500 | 0.604 | 0.701 | −11.2pp / −3.8pp |
| V4 XML | 0.299 | 0.470 | **0.634** | **0.761** | −14.2pp / **+2.2pp** |
| V5 HTML+前置标题 | 0.358 | 0.552 | 0.575 | 0.716 | −6.0pp / −2.3pp |
| V6 NL 摘要 | 0.284 | 0.440 | 0.321 | 0.455 | −17.2pp / −28.4pp |

（r@1/r@15/r@20/MRR 与四题型拆解在 metrics JSON 的 `worlds[w].merged` / `merged:{qtype}` 字段。）

## 2. 判据应用（机械，无裁量）

- 无变体双模型平均 +≥3pp；唯一单臂正增的 V4 在 qwen +2.2pp（未过线）且 bge −14.2pp（远超 −1pp 红线）→ **H1 否证，判据不满足 → 维持现状**。
- V4/V6 触发「任一模型反向劣化」→ **H2 触发：「格式选择须绑定模型」**（[ADR-0001 Consequences](docs/adr/0001-表格嵌入格式维持HTML.md)）。

## 3. 稳健性三查（全部同向）

| 查 | 查询集 | 与主集排序的关系 |
| --- | --- | --- |
| 敏感性 | 全量 240 问（含 36 条被闸剔除的「抄原文」问） | bge：V1 仍 @5/@10 最优；V4/V6 仍显著劣化（`metrics_relayall_bge.json`） |
| 生成源稳健 | 会话模型集 240 问 | bge：同上（`metrics_session_bge.json`） |
| 模型稳健 | — | qwen 主集上 V1 仍为总量最优之一；V6 依旧崩（`metrics_relay_qwen.json`） |

统计边界：n=134 时比例标准误 ≈4pp。**只有超出 ~2×SE（约 ±8pp）的差值可下强结论**：V4-bge（−14pp）、V6 双臂（−17/−28pp）、V3-bge（−11pp）为真效应；V2/V5 与 V1 的差（−2~−6pp）处于边缘，不做单独解读——但这不影响判定，因为判定只需要「无人显著超过 V1」。

## 4. 为什么出现这个结果（机理分析）

### 4.1 为什么 V1（原样 HTML）赢——三个机制

**① 内容零损失 + 词法锚定完整。** 查询虽然被强制改写，但表头词、行词、数值、日期等实体仍与单元格原文大面积共现。检索召回的第一驱动是锚点词匹配，而 V1 是唯一**逐格保留全部原文、且保持行列邻近关系**的变体。任何「转换」都要么稀释、要么碎片化、要么丢失内容（下文逐个说）。

**② 标记的代价不是「有没有标签」，而是「标签在不在分布内」。** 反常识的关键点：V4（XML）与 V1（HTML）内容完全相同，唯一差别是标签词表与属性（`<row r="1"><cell c="2">` vs `<tr><td colspan="2">`），在 bge 上却差 **14pp**。解释：bge-m3（560M XLM-R 系）的训练语料里有海量真实 HTML，`<td>`/`<tr>` 是高频、有稳定表示的 token——**熟悉的标记近乎免费**；而 `<row r="…">`、`c="…"` 这类自造属性是分布外序列，被切成分散子词、挤占表征预算。所以「标签噪声」论的对错取决于**标签是否在模型训练分布内**，而非标签本身。这与裁定阶段论文里「xml 常胜、shuffled rows 常败」的模型异质性观察同源。
qwen3-vl-embedding-8b（8B LLM 系、4096 维）对标记鲁棒得多（V4 只比 V1 好/持平），且显式坐标（`c=`/`r=`）给了它额外的定位信号——所以 XML 在它那里小幅占优。

**③ 扁平化的两类系统性损伤。** V2（展开管道）把合并区复制成重复值（「华东 华东」），表头词频被稀释、合并分组关系反而消失；V3（KV 平铺）把表头路径在每行重复一遍，表头词的文档内频率膨胀 N 倍、行内聚性被打散、数值被埋进「键: 值」模板。两者在 bge 上分别 −2pp 与 −11pp——**结构重构的代价超过格式收益**。局部例外印证机制：V3 在 bge 的「表头语义定位型」问题上反而最好（0.70 vs V1 0.62）——「某行某列」式问题恰好受益于路径键，但该优势撑不起全局。

### 4.2 为什么 V6（NL 摘要）双臂皆崩——粒度错配

摘要是**有损压缩**：2-5 句只保留 2-4 个「关键数值」，而本语料的查询是公文场景的**细胞级事实问句**（某行某列的值、两个格子比大小、一行求和）。没进摘要的格子等于从向量里消失了——查询锚点（具体数值/行名/表头词）无处匹配。四题型上 V6 全面垫底（paraphrase 0.50/0.47 vs V1 0.68/0.82），不是措辞问题（生成已双异源），是**内容覆盖问题**。Unstructured 的「摘要进向量」实践隐含的查询分布是粗粒度主题问句；对细胞级事实查询不迁移。**教训：检索嵌入文本的信息覆盖下界由查询粒度决定，不由摘要质量决定。**

### 4.3 为什么 bge 与 qwen 绝对水平差一大截

qwen 臂全变体 r@10 ≈0.70-0.76 vs bge ≈0.47-0.61：8B LLM 系嵌入器（4096 维、32k 上下文）对中文表格语义匹配显著强于 560M 编码器。这对副决策（选型）是直接输入；对主决策无影响（判定用的是同臂内相对差）。

### 4.4 与论文结论的整合

arXiv 2604.24040 证明「格式敏感」（格式→embedding 几何→排序，一阶变量）——本实验**复现了敏感性**（V1↔V4 在 bge 上差 14pp，同一内容）；但敏感性 ≠ 存在更优格式。论文自己就没有统一推荐格式（MPNet 上 html 在 WTQ 最差、NQ-Tables 最好），本实验在其上补了缺失的一环：**在你自己的语料和模型上，最安全的基本盘是「原样保留内容、使用模型分布内熟悉的标记」——而现状恰好就是这条**。

## 5. 结论边界（不许可的推论）

1. 只裁决了 merged 表的嵌入输入格式；**承载格式不动**（HTML 保 span 的规格级依据独立成立）。
2. 纯稠密向量口径：生产是 hybrid+rerank，BM25 路会奖励词法丰富变体（V2/V3），绝对值不可搬到生产；rerank 可能缩小变体差，但不会逆转 14-28pp 级的劣化。
3. qwen 臂用的是中转 **VL 多模态版**，与生产 `:9015` 的 Qwen3-Embedding-8B 非同一权重——「XML 对 qwen 更好」的外推需在目标权重上重测。
4. 查询是生成集：真实日志校准（issues8 A）完成前，本结论对真实查询分布的代表性是「三份生成集互证」，不是日志级证明。
5. 结论域 = 红头公文语料 + 该两模型 + 细胞级事实查询分布。换任何一项，按 [ADR-0001](docs/adr/0001-表格嵌入格式维持HTML.md) 约定重跑（`_embedlab/` 资产直接复用）。
