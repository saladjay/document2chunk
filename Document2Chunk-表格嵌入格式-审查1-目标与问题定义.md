# Document2Chunk-表格嵌入格式-审查包 1 · 目标与问题定义

## 1. 起点问题

一份外部调研摘要主张：① 表格序列化格式对 embedding 检索召回是一阶变量（引 arXiv 2604.24040，Recall@1 波动「可达 0.26」）；② HTML 表格能保合并单元格语义但**原始 HTML 不宜直接嵌入**，应转换（如 Markdown）后再嵌入。

经联网逐条裁定（[裁定与实验设计 §2-3](Document2Chunk-表格嵌入格式-裁定与实验设计.md)）：文献引用**真实且忠实**；但「标签 token 稀释语义权重」机制**无任何定量出处**（口传），「HTML vs Markdown 嵌入召回」**公开文献不存在头对头数字**（流传的 60.7%/53.6% 是 GPT 抽取基准错配）。→ **「直接嵌 HTML 还是转换后嵌」没有任何公开证据可裁决，只能用自有语料实验回答。**

> **审核修正注**：主张②（应转换后再嵌）出自**调研摘要与业界口传**；被引论文（arXiv 2604.24040）本身**未作此规范性主张**。本实验检验与否证的对象是前者，不是论文——与论文的关系见[审查4 §4.4](Document2Chunk-表格嵌入格式-审查4-结果与机理.md)。

## 2. 实验服务的决策

| 决策 | 谁消费 | 状态 |
| --- | --- | --- |
| **主决策**：markdown 导出中含合并格的表格 chunk（现状原样嵌出 HTML），嵌入输入格式要不要改 | `src/document2chunk/export/_helpers.py` 三分流 [导出实现](src/document2chunk/export/_helpers.py#L75) | ✅ 已判：不改（[ADR-0001](docs/adr/0001-表格嵌入格式维持HTML.md)） |
| 副决策：embedding 模型选型（bge-m3 vs qwen3-vl-embedding-8b） | 下游向量库选型 | 供输入：qwen 臂各 k 绝对值全面更高（见[审查4](Document2Chunk-表格嵌入格式-审查4-结果与机理.md)） |
| 结构性知识：格式×模型绑定、NL 摘要实践可迁移性 | 未来换模型/改管线的任何讨论 | 两条实证发现（[审查4 §4](Document2Chunk-表格嵌入格式-审查4-结果与机理.md)） |

**明确不裁决的**（范围排除，grill 四轮收窄的记录见 [审查1 附表](#5-附决策过程账本)）：
- 表格**承载格式**（HTML 保 colspan/rowspan 的结构保真）——由规格级证据独立支撑（pandoc/Docling 均声明管道表无 span；裁定报告 §2/§3），本实验不动它；
- Excel 轨A 行级 KV 序列化（双轨决策已裁决旧案，仅作对照不入统计）；
- 可编辑 PDF 路合并信息丢失（旧缺口，另案）。

## 3. 待检验假设与判定标准

**主假设 H1**：存在一种序列化变体，在两个生产候选 embedding 模型上都显著优于现状 HTML。
**备选发现 H2**：若各变体互有胜负且模型间方向不一致 → 「格式选择须绑定模型」成立。

**预登记判据**（2026-09-21 锁定，见下节证明；原文 [裁定与实验设计 §5.3](Document2Chunk-表格嵌入格式-裁定与实验设计.md)、[issues8 C 节](issues8.md)）：

> 某变体在**双模型**上 Recall@{5,10} 平均提升 ≥3 个百分点，且任一模型劣化 ≤1 个百分点 → 切换导出格式并立 ADR。
> 任一模型反向劣化 → 记「格式选择须绑定模型」发现，重议。

选 3pp 线与「双模型同时达标」的理由：本项目的教训是「召回100%会骗人」（[Excel轨A评比](Document2Chunk-Excel轨A-输出质量评比.md)），判据必须 conservative 且防单模型侥幸；阈值在见数前锁定防「跑完再找理由」。

## 4. 预登记的时间证明

- 2026-09-21（出数前日）：判据全文写入 [issues8.md C 节](issues8.md) 与 [裁定与实验设计 §5.3](Document2Chunk-表格嵌入格式-裁定与实验设计.md)，同期锁定变体网格 V1-V6、口径 Recall@{1,5,10,15,20}+MRR、样本量 40+20、真池要求。这两份文件的 git 提交时间早于全部评测产物（`_embedlab/results/*.json` 均为 09-21 深夜至 09-22 生成）。
- 判定环节无自由裁量：判据机械应用于 [审查4 §2](Document2Chunk-表格嵌入格式-审查4-结果与机理.md) 的表。

## 5. 附：决策过程账本（grill 四轮收窄记录）

| 轮 | 决策 | 定稿 |
| --- | --- | --- |
| R1 | 用途=裁定+决策+本地实验串联；裁定标准=拆条评级；口径=Recall@k+MRR（自建，仓库原无召回评测资产）；术语=区分「表格承载格式/嵌入输入格式」（[CONTEXT.md](CONTEXT.md)） | ✅ |
| R2 | 下游嵌入模型=bge-m3/qwen3-embedding-8b；下游协议可控（envelope 双表征可行）；服务取数窗口后实测 k=10（[easy-rag 配置](审查3-测试过程.md)） | ✅ |
| R3 | 双模型处于选型（实验兼模型对比）；轨A 不重开仅对照；k 网格 {1,5,10,15,20} | ✅ |
| R4 | 语料=生产同源 799 DOCX；查询=LLM 生成+强制改写+20% 人工抽查；变体网格 V1-V6（V6 为工程核查后预授权增补）；判据预登记 3pp/1pp | ✅ |
| 执行期 | qwen 臂改走 112 中转（用户指路，弃本地下载）；V6/查询改双异源生成（deepseek-v4 查询 + qwen3.6-35b 摘要，消除同源混杂） | ✅ |
