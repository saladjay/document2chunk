# Document2Chunk — Excel 解析难点定档表（双轨版 · Round 4 定稿）

> 定稿：2026-09-20（/grill-me Round 4）。频次来源：`Excel语料盘点-draft.md`（kxx-docs，N=23 个真实工作簿）。
> 处置语汇：**P0 正确处理**（错了答案就错）/ **P1 降级标记**（flag/告警但不出错）/ **P2 明确不支持**（文档写明，遇到报告）。
> 双轨：**轨 A** = chunk2embedding（行级 JSON，先行）；**轨 B** = text2sql 数据准备（schema+干净数据，二期）。
> 用途：① 开发排期清单（P0 → P1 → P2）；② 验收用例映射（语料文件 → 难点）；③ 后续优化 backlog（§3 合成用例、§4 P2 展望）。

---

## 1. 定档总表（28 项）

| # | 难点 | 真实频次 | 轨A | 轨B | 处置要点 | 覆盖用例（kxx-docs 语料） |
|---|---|---|---|---|---|---|
| 1 | 合并单元格 | **52%** | **P0** | **P0** | 表头建模与合并展开一体；数据区纵向合并值下放每行（不加标记） | `2.1-2022初步评审`(1424处,88处表头区)、`基础-最新`(3级表头+8处表头合并)、`3-无人机中台`(「路段」列纵向合并回填父值)、`提供材料清单`(跨行层级合并)、`初步评审结果模板`(43处全在表头区) |
| 2 | 一 sheet 多表/边界 | — | **P0** | **P0** | 空行空列切分 + 两条修正规则 | `初步评审结果模板`(空行分块) |
| 3 | 多行表头 | **43%** | **P0** | **P0** | 类型同质性+格式信号+header extension；防纯文本 sheet 误报（见 N5） | `基础-最新`(标准3级)、`2.1-2022初步评审`(双行)、`7-边坡巡查`(双行+科目3级)、`合规性成果提取`(嵌套合并) |
| 4 | 斜线表头/伪表头 | 0 | P2 | P2 | 合成用例 | 无 → 合成 |
| 5 | dimension 不可信 | 2 反向 | **P0** | **P0** | **以实际扫描值域为准**，声明只作提示（真实语料：0 例"声明虚胖"、2 例"声明小于实际"） | `8-中期报告`(B5 起始+声明 B5:I13 小于实际)、`5-智慧管养决算表`(声明 A2:E57 漏首行) |
| 6 | 隐藏行/列/sheet | 9% | P1 | P1 | 隐藏 sheet 跳过；隐藏行/列解析+meta.flag（检索影响上线后实验） | `2.1-2022初步评审`(4 隐藏列)、`签字名单(SheetJS)`(1 隐藏列)；隐藏行/sheet → 合成 |
| 7 | 公式缓存值 | **22%** | **P0** | **P0** | 取缓存值；**缺失→告警+保留 None，不静默填 0**（程序生成文件无缓存） | `项目清单2026.3`(1870 个公式)、`2.1-2022初步评审`(70 个)、`7-边坡巡查`(45 个) |
| 8 | 跨 sheet 引用 | ~0 | P1 | P1 | meta 层 `_ref` 标记 | 合成 |
| 9 | 外部链接 | 4% | P1 | P1 | 悬空检测+告警（值为缓存，可能 #REF!） | `项目清单2026.3`(悬空 `C:\...\360zip$Temp\...`) |
| 10 | defined names | 0 | P2 | P2 | | 合成 |
| 11 | 透视表 | 0 | P1 | P1 | zip 检测 `xl/pivotTables/` 即告警（值在 pivot cache，按 cell 读会整块丢） | 合成 |
| 12 | Power Query/宏 | 0 | P2 | P2 | | 合成(.xlsm) |
| 13 | 类型陷阱 | 未单列 | P1 | **P0** | 轨A 值规范化（ISO 日期/去千分位/保货币符）；轨B 类型推断低置信降级 TEXT+告警 | `项目清单2026.3`(40 列混合类型) |
| 14 | 空值五义 | — | P1 | P1 | None/空串/空格/`""`/合并阴影 归一化 | 合成 |
| 15 | 重复/空列名 | — | **P0** | **P0** | 撞名加`（2）`、空表头`列N`（丢列=静默丢数据） | 合成（多级表头展开撞名） |
| 16 | 条件格式当数据 | 0 | P2 | P2 | | 合成 |
| 17 | 批注/文本框数据 | 0 | P2 | P2 | | 合成 |
| 18 | 大表性能 | 0 | P1 | P1 | calamine 架构保底；合成 50 万行基准 | 合成 |
| 19 | 假扩展名/老格式 | **4%** | **P0** | **P0** | 魔数指纹识别 + soffice 转换兜底（**复用已上线 .doc 链路**） | `20-软弱地层`(OLE2 假 .xlsx，openpyxl 报 BadZipFile) |
| 20 | 加密文件 | 0 | P2 | P2 | 拒收+明确报错；接口文档写明"打开密码不作访问控制，下游网关层解密" | 合成 |
| 21 | WPS .et | 0 | P2 | P2 | 链路已备，本语料库无样本 | 无语料 |
| 22 | 超宽/转置表 | 宽表有 | P2 | P2 | 宽表行级 JSON 天然适配；转置 → 合成 | `项目清单2026.3`(40×938)、`科技项目清单`(277×34 规整对照) |
| N1 | 空 sheet | **13%** | **P0** | **P0** | 跳过不崩 + warnings 记录 | `附件3-1讲话精神`(2 空)、`科小星测试`(1×1 近空)、`政策汇编`(1 空) |
| N2 | `~$` 锁文件 | 2 | **P0** | **P0** | 入口 glob 排除（165 B，不排除会崩） | 2 个锁文件（落选清单） |
| N3 | 单元格内换行 | 9% | P1 | P1 | 键压单空格、值保留+text 压平 | `合规性成果提取`、`合规性`(多行要求文本) |
| N4 | csv 编码嗅探 | 无语料 | P1 | P1 | utf-8/gbk + 分隔符探测 | 合成 |
| N5 | 文档型纯文本 sheet | **22%** | **P0** | P1 | **"表格当文档"路线**（Q18=(a)）：不建表头，每行/每对转独立文本块；轨B 跳过+告警 | `常见问题汇总`、`附件1操作FAQ`、`附件3-1讲话精神`、`政策汇编`、`组织页面数据`(对照：不得误判多行表头) |
| N6 | 表单式 sheet | ~2 | **P0** | P1 | **整 sheet 聚合为单个文档块**（Q19=(a)）；轨B 跳过+告警（表单无法入库） | `7-广东高速决算表`(表尾合计公式)、`5-智慧管养决算表`(表单式合并表头)、`7-边坡巡查`(表中合计) |
| N7 | 第三方生成器兼容 | 1 | P1 | P1 | SheetJS 等非 Office 产物（无 styles 部件） | `签字名单(SheetJS)`(618 行) |

**P0 清点**：轨 A 11 项（#1,2,3,5,7,15,19,N1,N2,N5,N6）；轨 B 10 项（同上去掉 N5/N6，加 #13）。
**主战场结论**：#1+#3 合计覆盖 65% 真实文件，且合并几乎总是落在表头区（12/12）——表头建模与合并展开应做成一件事。

---

## 2. 验收通过标准（建议）

1. P0 用例全绿：21 个入选文件按盘点报告 §2 的覆盖点逐项验证；
2. 对照组不误判：`组织页面数据`(通讯录) 不得判多行表头；`科技项目清单`(规整宽表) 逐行 JSON 与原值全等；
3. 入口卫生：2 个 `~$` 锁文件 + 假 .xlsx 走指纹→soffice 链路不崩；
4. 人工核对：P0 文件行级 JSON 抽样比对（对齐 docx 验收模式）；
5. 轨 B 专项：`7-广东高速决算表` 剔除合计行后 `SUM()` 与原表合计一致（双计检验）。

---

## 3. 合成用例需求清单（零命中难点的后续优化材料）

真实语料零命中的难点**不能指望真实样本**，以下用例需手工制作，纳入回归库：

| 合成用例 | 覆盖难点 | 制作要点 |
|---|---|---|
| 幽灵 dimension（声明虚胖） | 5 | 对整列设格式后清除值，使声明维度远大于实际 |
| 透视表 workbook | 11 | 手工建 pivot；验证 zip 级检测告警 |
| chartsheet | 已知坑 | 验证不崩（Docling 曾在此崩溃） |
| defined names / 结构化引用 | 10 | 名称管理器 + `Table[列]` 引用 |
| .xlsm（VBA）/ .xlsb | 12/19 | 格式开关验证 |
| 加密文件 | 20 | 验证明确报错而非挂死/静默空输出 |
| .et（WPS） | 21 | soffice 归一化链路验证 |
| .csv（gbk / utf-8 / 分号分隔 / 无表头） | N4 | 编码+分隔符嗅探 |
| 隐藏 sheet / 隐藏行 | 6 | flag 政策两侧验证 |
| 斜线表头 / 条件格式 / 批注文本框 / 转置表 | 4/16/17/22 | 各做最小样本 |
| 程序生成无缓存公式 | 7 | openpyxl 写公式不存缓存 → 验证 None+告警不填 0 |
| 50 万行宽表 | 18 | 性能基准（calamine vs openpyxl 计时，内存曲线） |
| 一 sheet 三表（横+纵混排） | 2 | 修正规则 ①② 的边界验证 |

---

## 4. 后续优化 backlog（P2 项，按需启动）

- **#16 条件格式语义**：如业务出现"红色=逾期"类语义表，评估读取 conditionalFormatting 规则；
- **#17 批注/文本框**：如签字名单/评审表出现批注数据，评估读 `xl/comments.xml`；
- **#10 defined names**：出现命名区域报表时再做；
- **#12 Power Query**：出现"数据是查询生成的"需求时，提示用户导出值而非解析查询；
- **#4 斜线表头**：国内预算表特有，出现时用"斜线单元格=多键拆分"策略；
- **N7 深化**：收集更多第三方生成器样本（SheetJS/et/集算导出）。

---

## 5. 关联文档

- 主报告（双轨架构与全部设计决策）：`Document2Chunk-Excel解析技术调研报告.md`
- 语料盘点（频次与逐文件测试理由）：`Excel语料盘点-draft.md`
- 扫描明细：`_forensics/excel_corpus_scan.json`；脚本：`scripts/_corpus_scan_excel.py`
- text2sql 数据准备调研（二期，已产出草稿）：`Document2Chunk-text2sql数据准备调研-draft.md`
